name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python manage.py check
        env: {SECRET_KEY: ci-secret-key, DEBUG: "1"}
      - run: python manage.py test
        env: {SECRET_KEY: ci-secret-key, DEBUG: "1"}
